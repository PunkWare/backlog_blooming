<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
	<!-- Call the partial _user (in the same folder) for each user in @users -->
	<%= render @users %>
</ul>

<%= will_paginate %>